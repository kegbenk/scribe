---
name: Bug report
about: Extraction produced wrong output, crashed, or failed on a document
labels: bug
---

**What happened**

<!-- What did Scribe produce, and what did you expect? -->

**Document**

<!-- Format (PDF/EPUB), size, and kind (digital, scanned, single-file EPUB…).
     Attach the document or a minimal reproducing sample if you can share it —
     most extraction bugs can't be fixed without one. If it's private, note
     which corpus book (corpus/) comes closest. -->

**Environment**

- Scribe version / commit:
- OS (macOS/iOS + version):
- Entry point (library `extractContent`, `scribe-cli`, other):

**Output**

<!-- Relevant slice of the contentStructure JSON, CLI stderr, or a crash log. -->
